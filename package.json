{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Kaushik R Bangera — portfolio site, deployed to Cloudflare Workers (static assets).",
  "scripts": {
    "dev": "wrangler dev --env prod",
    "dev:test": "wrangler dev --env test",
    "deploy": "wrangler deploy --env prod",
    "deploy:prod": "wrangler deploy --env prod",
    "deploy:test": "wrangler deploy --env test"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
