{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Kaushik R Bangera — portfolio site, deployed to Cloudflare Workers (static assets).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.93.1"
  }
}
